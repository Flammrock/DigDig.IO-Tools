import { AppRemoteApi } from 'shared'

export const remoteApi: AppRemoteApi = {
  // add main function here
  // functions listed here can be called when a tool is mounted
  // to start some server or other thing that cannot be usually done
  // from a browser but in a nodejs instance instead
}
